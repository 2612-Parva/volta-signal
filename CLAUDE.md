# Volta Signal

Monthly founders newsletter. Timezone America/Halifax.

- Do not auto-send without Slack approval
- Do not invent sources
- Do not put secrets in git, logs, Slack, or `/architecture`
- Mailchimp drafts are not sent
- Send test is not Approve and send

Manual run: Slack `/volta publish`. Send is the card, not the command.
Use skills `volta-signal-publish` and `volta-signal-send`. Facts live in `memory/`.
