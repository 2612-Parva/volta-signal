{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Tests run on Node, so they need Node's types alongside the Worker types
    // used by the modules under test.
    "types": ["node", "@cloudflare/workers-types"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
